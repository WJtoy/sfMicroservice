<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.kkl.kklplus.b2b.sf.mapper.SFOrderHandleMapper">

    <insert id="insert" keyProperty="id" useGeneratedKeys="true">
        insert into sf_order_handle(
            b2b_order_id,
            waybill_no,
            task_code,
            verify_cancel_code,
            unique_id,
            operate_code,
            operate_time,
            install_master,
            install_contact,
            exception_code,
            content,
            city,
            app_time,
            process_flag,
            process_time,
            process_comment,
            create_by,
            create_date,
            update_by,
            update_date,
            quarter
            )values (
            #{b2bOrderId},
            #{waybillNo},
            #{taskCode},
            #{verifyCancelCode},
            #{uniqueId},
            #{operateCode},
            #{operateTime},
            #{installMaster},
            #{installContact},
            #{exceptionCode},
            #{content},
            #{city},
            #{appTime},
            #{processFlag},
			#{processTime},
			#{processComment},
			#{createById},
			#{createDt},
			#{updateById},
			#{updateDt},
			#{quarter}
            )
    </insert>


    <update id="updateProcessResult">
		UPDATE
			sf_order_handle
		SET
			process_flag = #{processFlag},
			process_time = process_time+1,
			process_comment = #{processComment},
			update_date = #{updateDt}
	  	WHERE
	  		id = #{id}
	</update>

</mapper>
